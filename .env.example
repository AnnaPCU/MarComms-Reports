# ── Login compartido (auth simple, un solo rol) ───────────────────
# Si falta, se usa el fallback 'marcomms2026'.
VITE_SHARED_PASSWORD=marcomms2026

# La app NO usa base de datos ni backend: los datos viven en el seed en código
# (src/data/*Seed.js) y viajan en el bundle. No hacen falta más variables.
# (Supabase fue descartado — ver PROJECT_CONTEXT.md §10.)
